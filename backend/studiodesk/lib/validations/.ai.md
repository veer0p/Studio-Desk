# Module: lib/validations
**Path**: `/home/veer/Documents/Projects/Studio-Desk/backend/studiodesk/lib/validations`
**Last Updated**: 2026-04-12T12:00:00.000Z
**Language**: TypeScript (Zod)

## Purpose
Zod validation schemas for all API inputs, query parameters, and request bodies. 53 total schemas across 15 files.

## Key Schemas

### Auth (`auth.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `loginSchema` | User login | `email`, `password` (min 8) |
| `signupSchema` | Studio owner signup | `name`, `email`, `password` (min 8), `studioName` |
| `forgotPasswordSchema` | Forgot password | `email` |
| `resetPasswordSchema` | Password reset | `password` (min 8) |
| `updatePasswordSchema` | Password change | `currentPassword`, `newPassword` (min 8) |
| `inviteMemberSchema` | Team invite | `email`, `role` |
| `updateMemberRoleSchema` | Role update | `role` |

**Called By** (Level 1):
- `AuthService` → `../services/.ai.md`

### Booking (`booking.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `createBookingSchema` | Create booking | `client_id`, `event_date`, `event_type`, `title`, `total_amount?` |
| `updateBookingSchema` | Update booking | Partial create + `status` |
| `bookingQuerySchema` | Query params | `status?`, `event_type?`, `search?`, `from_date?`, `to_date?`, `page?`, `limit?` |

**Called By** (Level 1):
- `BookingService` → `../services/.ai.md`

### Client (`client.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `createClientSchema` | Create client | `full_name`, `phone` (Indian regex), `email?`, `whatsapp?`, `tags?` |
| `updateClientSchema` | Update client | Partial create |
| `clientQuerySchema` | Query params | `search?`, `source?`, `page?`, `limit?` |

**Data Schema**:
```typescript
interface CreateClientInput {
  full_name: string;
  phone: string;
  email?: string;
  whatsapp?: string;
  address?: string;
  city?: string;
  state?: string;
  state_id?: string;
  pincode?: string;
  company_name?: string;
  gstin?: string;
  notes?: string;
  source?: 'referral' | 'social_media' | 'google' | 'website' | 'repeat' | 'other';
  tags?: string[];
}
interface UpdateClientInput extends Partial<CreateClientInput> {}
```

**Called By** (Level 1):
- `ClientService` → `../services/.ai.md`

### Invoice (`invoice.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `createInvoiceSchema` | Create invoice | `booking_id`, `invoice_number`, `line_items[]`, `due_date` |
| `updateInvoiceSchema` | Update invoice | Partial create + `status` |
| `addPaymentSchema` | Record payment | `invoice_id`, `amount`, `payment_method`, `date` |
| `invoiceQuerySchema` | Query params | `status?`, `search?`, `from_date?`, `to_date?` |

**Called By** (Level 1):
- `InvoiceService` → `../services/.ai.md`

### Lead (`lead.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `createLeadSchema` | Create lead | `name`, `email?`, `phone?`, `event_type?`, `budget?`, `source?` |
| `updateLeadSchema` | Update lead | Partial create + `stage` |
| `convertLeadSchema` | Convert to booking | `lead_id`, `client_data?`, `booking_data` |
| `leadQuerySchema` | Query params | `stage?`, `source?`, `search?` |

**Called By** (Level 1):
- `LeadService` → `../services/.ai.md`

### Contract (`contract.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `createContractSchema` | Create contract | `booking_id`, `title`, `content` |
| `updateContractSchema` | Update contract | Partial + `status` |
| `signContractSchema` | Sign contract | `contract_id`, `signature_data` (base64) |
| `contractQuerySchema` | Query params | `booking_id?`, `status?`, `search?` |

**Called By** (Level 1):
- `ContractService` → `../services/.ai.md`

### Gallery (`gallery.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `createGallerySchema` | Create gallery | `booking_id`, `title`, `access_type` |
| `updateGallerySchema` | Update gallery | Partial + `is_active` |
| `galleryQuerySchema` | Query params | `booking_id?`, `access_type?` |

**Called By** (Level 1):
- `GalleryService` → `../services/.ai.md`

### Package (`package.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `createPackageSchema` | Create package | `name`, `description?`, `price`, `duration_hours?`, `category?` |
| `updatePackageSchema` | Update package | Partial create |
| `packageQuerySchema` | Query params | `category?`, `is_active?`, `search?` |

**Called By** (Level 1):
- `PackageService` → `../services/.ai.md`

### Proposal (`proposal.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `createProposalSchema` | Create proposal | `lead_id`, `title`, `content`, `packages?`, `custom_items?` |
| `updateProposalSchema` | Update proposal | Partial + `status` |
| `proposalActionSchema` | Client accept/reject | `proposal_id`, `action` (accept/reject), `message?` |

**Called By** (Level 1):
- `ProposalService` → `../services/.ai.md`

### Assignment (`assignment.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `createAssignmentSchema` | Assign team | `booking_id`, `user_id`, `role` (enum: photographer, videographer, editor, assistant) |
| `updateAssignmentSchema` | Update assignment | `role?`, `notes?`, `is_active?` |

**Called By** (Level 1):
- `AssignmentService` → `../services/.ai.md`

### Automation (`automation.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `createAutomationSchema` | Create automation | `name`, `trigger`, `action`, `template`, `enabled?` |
| `updateAutomationSchema` | Update automation | Partial create |
| `testAutomationSchema` | Test automation | `id` |

**Called By** (Level 1):
- `AutomationService` → `../services/.ai.md`

### Settings (`settings.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `studioSettingsSchema` | Studio config | `studio_name`, `timezone`, `currency`, `brand_color?`, `logo_url?` |
| `emailSettingsSchema` | Email config | `from_name`, `from_email`, SMTP optional |
| `paymentSettingsSchema` | Payment config | `razorpay_key_id?`, `razorpay_key_secret?`, `gst_number?`, `gst_rate?` |
| `notificationSettingsSchema` | Notification toggles | `email_booking_confirm?`, `sms_enabled?`, `whatsapp_enabled?` |

**Called By** (Level 1):
- `SettingsService` → `../services/.ai.md`

### Studio (`studio.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `createStudioSchema` | Create studio | `name`, `owner_name`, `email`, `password` (min 8), `plan?` |
| `updateStudioSchema` | Update profile | `name?`, `description?`, `website?`, `phone?`, `address?`, `city?`, `state?`, `pincode?` |

**Called By** (Level 1):
- `StudioService` → `../services/.ai.md`

### Team (`team.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `inviteTeamMemberSchema` | Invite member | `email`, `role` (manager/editor/shooter), `message?` |
| `updateTeamMemberSchema` | Update member | `role?`, `is_active?` |
| `acceptInviteSchema` | Accept invite | `token`, `password` (min 8), `name` |

**Called By** (Level 1):
- `TeamService` → `../services/.ai.md`

### Common (`common.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `paginationSchema` | Reusable pagination | `page` (min 1, default 1), `limit` (min 1, max 100, default 20) |
| `sortOrderSchema` | Reusable sort | `sort?`, `order` (asc/desc) |
| `dateRangeSchema` | Reusable date range | `from`, `to` |
| `idParamSchema` | UUID param | `id` (uuid) |
| `idListSchema` | UUID list | `ids[]` (uuid[]) |

### Admin (`admin.schema.ts`)
| Schema | Purpose | Key Fields |
|---|---|---|
| `adminLoginSchema` | Admin login | `email` (email), `password` (required) |
| `auditLogQuerySchema` | Audit log filters | `admin_id?`, `entity_type?`, `action?`, pagination |
| `studioListQuerySchema` | Studio list filters | `search?`, `plan_tier?`, `subscription_status?`, `is_suspended?` |
| `createFeatureFlagSchema` | Create feature flag | `flag_name` (lowercase_underscore), `is_enabled`, `enabled_for_tiers` |
| `updateFeatureFlagSchema` | Update flag | `is_enabled?`, `enabled_for_tiers?` |
| `updatePlatformSettingSchema` | Update setting | `value?`, `value_json?` |
| `suspendStudioSchema` | Suspend studio | `reason` (min 10 chars) |
| `impersonateStudioSchema` | Impersonate studio | `reason` (min 10 chars) |
| `createSupportNoteSchema` | Add support note | `note` (max 5000), `is_flagged?` |

**Called By** (Level 1):
- Nearly all API routes → `../../app/api/v1/.ai.md`
- `AutomationService` → `../services/.ai.md`
- `SettingsService` → `../services/.ai.md`

## Common Patterns
- All schemas use Zod's `.safeParse()` in services for validation
- Type inference via `z.infer<typeof schemaName>` in service layers
- Common schemas (pagination, dateRange) imported across multiple services

## Related Modules
- Services: `../services/.ai.md`
- API Routes: `../../app/api/v1/.ai.md`

## Recent Changes
- 2026-04-12T12:00:00Z: Full rewrite with Called By sections, Data Schema blocks
