# Module: lib (utilities)
**Path**: `/home/veer/Documents/Projects/Studio-Desk/backend/studiodesk/lib`
**Last Updated**: 2026-04-12T12:00:00.000Z
**Language**: TypeScript

## Purpose
Cross-cutting utilities: AES-256-GCM encryption, error factory, DB logging, rate limiting, INR formatting, response helpers, and env validation.

## Key Functions

### `encrypt(plaintext)` / `decrypt(ciphertext)`
**File**: `crypto.ts`
**Purpose**: AES-256-GCM encryption for sensitive stored data (API keys, credentials)
**Parameters**: `{ plaintext: string }` | `{ ciphertext: string }`
**Returns**: `string` (format: `iv:authTag:encrypted` hex)
**Async**: No

**Calls To** (Level 1):
- `env` → `./env.ts`

**Called By** (Level 1):
- `SettingsService` → `./services/.ai.md`
- `StudioService` → `./services/.ai.md`
- `ImmichService` → `./services/.ai.md`

### `generateSecureToken(bytes?)`
**File**: `crypto.ts`
**Purpose**: Generate cryptographically secure random tokens (default 32 bytes = 64 hex chars)
**Parameters**: `{ bytes?: number }` (default 32)
**Returns**: `string` (hex)
**Async**: No

**Called By** (Level 1):
- `TeamService` → `./services/.ai.md`
- `ProposalService` → `./services/.ai.md`
- `GalleryService` → `./services/.ai.md`

### `hashToken(token)`
**File**: `crypto.ts`
**Purpose**: SHA256 hash for storing session tokens
**Parameters**: `{ token: string }`
**Returns**: `string` (hex digest)
**Async**: No

### `ServiceError` (class)
**File**: `errors.ts` [line:1]
**Purpose**: Typed error with message, code, and HTTP status
**Parameters**: `{ message: string, code: string, status?: number }`

### `Errors` (factory object)
**File**: `errors.ts` [line:1]
**Purpose**: Pre-configured error creators for common HTTP statuses
**Methods**: `notFound(entity)`, `unauthorized()`, `forbidden()`, `conflict(msg)`, `quotaExceeded()`, `paymentFailed(msg)`, `validation(msg)`, `external(service)`, `rateLimited()`

**Called By** (Level 1):
- All services → `./services/.ai.md`
- All repositories → `./repositories/.ai.md`
- `checkAndIncrementRateLimit` → `./rate-limit.ts`
- `[ORPHAN - review needed]` No orphan functions

### `logError(params)`
**File**: `logger.ts`
**Purpose**: Log errors to DB with severity, context, and PII redaction
**Parameters**: `{ message, stack?, studioId?, userId?, severity?, requestUrl?, context? }`
**Returns**: `void`
**Async**: Yes

**Calls To** (Level 1):
- `createAdminClient` → `./supabase/.ai.md`

**Called By** (Level 1):
- `AutomationService` → `./services/.ai.md`
- `GalleryService` → `./services/.ai.md`
- `InvoiceService` → `./services/.ai.md`
- `LeadService` → `./services/.ai.md`
- `ProposalService` → `./services/.ai.md`
- `TeamService` → `./services/.ai.md`

### `logSecurityEvent(params)`
**File**: `logger.ts`
**Purpose**: Log security events (auth failures, permission changes) with IP/UA
**Parameters**: `{ eventType, req, studioId?, userId?, context? }`
**Returns**: `void`
**Async**: Yes

**Calls To** (Level 1):
- `createAdminClient` → `./supabase/.ai.md`

**Called By** (Level 1):
- `TeamService` → `./services/.ai.md`

### `checkAndIncrementRateLimit(key)`
**File**: `rate-limit.ts`
**Purpose**: Rate limiting via DB-backed sliding window (10 hits/hour)
**Parameters**: `{ key: string }`
**Returns**: `void` | throws `RATE_LIMITED`
**Async**: Yes

**Called By** (Level 1):
- `GalleryService` → `./services/.ai.md`

### `checkInquiryRateLimit(key)`
**File**: `rate-limit.ts`
**Purpose**: Stricter rate limit for inquiry form (5 hits/hour/studio)
**Parameters**: `{ key: string }`
**Returns**: `void` | throws `RATE_LIMITED`
**Async**: Yes

**Called By** (Level 1):
- `LeadService` → `./services/.ai.md`

### `Response` (object)
**File**: `response.ts`
**Purpose**: Standardized Next.js API response helpers
**Methods**:
- `ok<T>(data, status?)` → 200 `{ data, error: null }`
- `created<T>(data)` → 201
- `noContent()` → 204
- `error(message, code?, status?)` → `{ data: null, error, code }`
- `paginated<T>(data, count, page, pageSize)` → paginated envelope with meta

**Called By** (Level 1):
- All API routes → `../../app/api/v1/.ai.md`

### `env`
**File**: `env.ts`
**Purpose**: Zod-validated environment variables (Supabase, Razorpay, email, Immich, encryption key, WhatsApp)
**Type**: Zod-inferred object with 15+ required keys

**Called By** (Level 1):
- Nearly all service/utility modules

### `formatINR(amount)` / `formatINRShort(amount)`
**File**: `formatters.ts`
**Purpose**: Indian Rupee currency formatting (`₹12,500.00` / `₹15L`)
**Parameters**: `{ amount: number | string }`
**Returns**: `string`
**Async**: No

**Called By** (Level 1):
- `contract.service.support.ts` → `./services/.ai.md`

### `formatIndianDate(date)` / `formatIndianDateTime(date)` / `formatRelativeTime(date)`
**File**: `formatters.ts`
**Purpose**: IST timezone date formatting
**Parameters**: `{ date: Date | string }`
**Returns**: `string`
**Async**: No

**Called By** (Level 1):
- `AutomationService` → `./services/.ai.md`
- `contract.service.support.ts` → `./services/.ai.md`

## Related Modules
- Services: `./services/.ai.md`
- Repositories: `./repositories/.ai.md`
- API Routes: `../../app/api/v1/.ai.md`

## Recent Changes
- 2026-04-12T12:00:00Z: Full rewrite with line numbers, Called By sections
