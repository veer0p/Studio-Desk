# Module: lib/supabase
**Path**: `/home/veer/Documents/Projects/Studio-Desk/backend/studiodesk/lib/supabase`
**Last Updated**: 2026-04-12T12:00:00.000Z
**Language**: TypeScript

## Purpose
Supabase client factory functions for different authentication contexts (user, admin, browser, server) and common query helpers.

## Key Functions

### `createAdminClient()`
**File**: `admin.ts`
**Purpose**: Creates Supabase client with service role key (bypasses RLS)
**Parameters**: `{}`
**Returns**: `SupabaseClient`
**Async**: No

**Called By** (Level 1):
- Nearly all services → `../services/.ai.md`
- Nearly all repositories → `../repositories/.ai.md`
- `logError`, `logSecurityEvent` → `../.ai.md`
- `checkAndIncrementRateLimit` → `../.ai.md`
- `galleryRepo` → `../repositories/.ai.md`

### `createClientFromAccessToken(accessToken)`
**File**: `server.ts`
**Purpose**: Creates Supabase client from JWT Bearer token (for API tests)
**Parameters**: `{ accessToken: string }`
**Returns**: `SupabaseClient`
**Async**: No

### `createClient()`
**File**: `server.ts`
**Purpose**: Creates Supabase SSR client with cookie auth (Server Components)
**Parameters**: `{}`
**Returns**: `SupabaseClient`
**Async**: No

**Called By** (Level 1):
- Auth API routes → `../../app/api/v1/.ai.md`

### `createBrowserClient()`
**File**: `client.ts`
**Purpose**: Creates browser-side Supabase client (client components)
**Parameters**: `{}`
**Returns**: `SupabaseClient`
**Async**: No

### `getMemberByUserId(supabase, userId)`
**File**: `queries.ts`
**Purpose**: Looks up studio member by user ID, filters on `is_active = true`
**Parameters**: `{ supabase: SupabaseClient, userId: string }`
**Returns**: `{ studio_id: string, role: string, member_id: string } | null`
**Async**: Yes

**Called By** (Level 1):
- `AuthService` → `../services/.ai.md`

### `getBookingById(supabase, bookingId, studioId)`
**File**: `queries.ts`
**Purpose**: Fetches booking scoped to studio. Throws `NOT_FOUND` if missing.
**Parameters**: `{ supabase, bookingId, studioId }`
**Returns**: `Booking` | throws
**Async**: Yes

**Called By** (Level 1):
- `ProposalService` → `../services/.ai.md`

### `getClientById(supabase, clientId, studioId)`
**File**: `queries.ts`
**Purpose**: Fetches client scoped to studio. Throws `NOT_FOUND` if missing.
**Parameters**: `{ supabase, clientId, studioId }`
**Returns**: `Client` | throws
**Async**: Yes

**Called By** (Level 1):
- `ProposalService` → `../services/.ai.md`

### `checkStorageQuota(supabase, studioId, uploadSizeGb)`
**File**: `queries.ts`
**Purpose**: Validates storage quota not exceeded
**Parameters**: `{ supabase, studioId, uploadSizeGb }`
**Returns**: `void` | throws `QUOTA_EXCEEDED`
**Async**: Yes

## Related Modules
- Services: `../services/.ai.md`
- Repositories: `../repositories/.ai.md`
- Types: `../../types/.ai.md`

## Recent Changes
- 2026-04-12T12:00:00Z: Full rewrite with Called By sections
