# Module: tests
**Path**: `/home/veer/Documents/Projects/Studio-Desk/backend/studiodesk/tests`
**Last Updated**: 2026-04-12T12:30:00.000Z
**Language**: TypeScript (Vitest)

## Purpose
Vitest test suite — 79 files covering API routes (mocked), services (unit), integration (real DB), and deep E2E smoke tests. ~572 test cases total.

## Test Layers

| Layer | Folder | Files | Tests | DB | Auth |
|---|---|---|---|---|---|
| **Unit** | `unit/` | 22 | ~163 | ❌ Mocked | ❌ |
| **API (mocked)** | `api/` | 9 | ~84 (28 skipped) | ❌ Mocked | ✅ Mocked |
| **Integration** | `integration/` | 18 + 4 helpers | ~311 | ✅ Real | ✅ Real tokens |
| **Deep** | `deep/` | 17 (12 re-exports) | ~14 unique | ✅ Real | ✅ Real tokens |

## Key Setup

### `setup.ts`
**Purpose**: Loads `.env.test`, pings local Supabase REST API in `beforeAll`
**Imports**: `vitest`, `dotenv`

### Test Helpers (`helpers/`)
| File | Exports | Purpose |
|---|---|---|
| `api.ts` | `api(method, path, options)` | Generic API runner via `testApiHeader`. Adds Bearer token. |
| `request.ts` | `makeRequest(method, path, options)` | Same as api.ts but for integration tests. Auto-sets Content-Type. |
| `auth.ts` | `getOwnerToken()`, `getPhotographerToken()`, `getEditorToken()`, `getOutsiderToken()` | Signs in 4 test users (password: `Test@1234`), caches tokens. |
| `tokens.ts` | `setupTokens()`, `tokens` object | Legacy token helper for `deep/` tests. |
| `db.ts` | `getRow()`, `getCount()`, `getRaw(sql)` | DB assertion helpers. `getRaw` uses direct pg connection. |
| `resolve-route.ts` | `loadAppRoute(pathname)` | Registry of ~90 URL patterns mapped to dynamic `import()` of Next.js route handlers. |

### Integration Fixtures (`integration/helpers/`)
| File | Exports | Purpose |
|---|---|---|
| `assignment-fixtures.ts` | `resetAssignmentFixtures()` | Seeds bookings, shoot_assignments, shoot_briefs, member_unavailability |
| `automation-fixtures.ts` | `resetAutomationFixtures()` | Seeds automation_settings, templates, automation_log |
| `gallery-fixtures.ts` | `resetGalleryFixtures()` | Seeds galleries, face_clusters, gallery_photos, file_upload_jobs |
| `invoice-fixtures.ts` | `resetInvoiceFixtures()` | Seeds invoices, line_items, payments, bookings |

## Related Modules
- Services: `../lib/services/.ai.md`
- Repositories: `../lib/repositories/.ai.md`
- API Routes: `../app/api/v1/.ai.md`
- Supabase: `../lib/supabase/.ai.md`
