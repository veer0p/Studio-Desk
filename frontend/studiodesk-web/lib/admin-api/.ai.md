# Module: lib/admin-api
**Path**: `/home/veer/Documents/Projects/Studio-Desk/frontend/studiodesk-web/lib/admin-api.ts`
**Last Updated**: 2026-04-12T14:00:00.000Z
**Language**: TypeScript

## Purpose
Admin API client — typed fetchers for all platform administration endpoints.

## Key Functions

### Auth
| Function | Parameters | Returns | Endpoint |
|---|---|---|---|
| `adminLogin(email, password)` | Credentials | `{ admin, session_token }` | POST `/api/v1/admin/auth/login` |
| `adminLogout()` | None | void | POST `/api/v1/admin/auth/logout` |
| `getAdminMe()` | None | `AdminRecord` | GET `/api/v1/admin/auth/me` |

### Dashboard
| Function | Parameters | Returns | Endpoint |
|---|---|---|---|
| `fetchAdminDashboard()` | None | `AdminDashboardData` | GET `/api/v1/admin/dashboard` |

### Studios
| Function | Parameters | Returns | Endpoint |
|---|---|---|---|
| `fetchAdminStudios(params)` | `{ search?, plan_tier?, subscription_status?, is_suspended?, page?, pageSize? }` | `{ studios: StudioRecord[], meta }` | GET `/api/v1/admin/studios` |
| `fetchAdminStudioDetail(id)` | `{ id: string }` | `StudioDetailData` | GET `/api/v1/admin/studios/[id]` |
| `suspendStudio(id, reason)` | `{ id, reason }` | `{ suspended, studio }` | POST `/api/v1/admin/studios/[id]/suspend` |
| `reactivateStudio(id, reason)` | `{ id, reason }` | `{ reactivated, studio }` | POST `/api/v1/admin/studios/[id]/reactivate` |
| `impersonateStudio(id, reason)` | `{ id, reason }` | `{ impersonation_id, studio, message }` | POST `/api/v1/admin/studios/[id]/impersonate` |

### Types
| Type | Fields |
|---|---|
| `AdminRecord` | `id`, `name`, `email`, `role`, `is_active`, `login_count`, `last_login_at` |
| `StudioRecord` | `id`, `name`, `slug`, `plan_tier`, `subscription_status`, `is_active`, `is_suspended`, `storage_*` |
| `StudioDetailData` | `studio`, `members[]`, `stats`, `support_notes[]` |
| `AdminDashboardData` | `overview`, `health` |

**Called By** (Level 1):
- Admin pages → `../../app/(admin)/.ai.md`
- Admin hooks → `../../hooks/.ai.md`

## Related Modules
- Backend admin routes: `/home/veer/Documents/Projects/Studio-Desk/backend/studiodesk/app/api/v1/.ai.md`
- Hooks: `../../hooks/.ai.md`
