/**
 * PDF Service — Stub implementation for MVP.
 * Full implementation in Tier 2 with @react-pdf/renderer.
 */
export const PdfService = {
  async generateProposalPdf(proposalId: string): Promise<string | null> {
    // TODO: Implement with @react-pdf/renderer
    // Generate PDF, upload to Supabase storage, return public URL
    return null
  }
}
